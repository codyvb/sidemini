import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Files in the public/.well-known directory will be served at /.well-known/
  // No need for rewrites as files in public are served directly
};

export default nextConfig;
